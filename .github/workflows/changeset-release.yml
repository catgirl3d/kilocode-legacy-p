name: Changeset Release

on:
    push:
        branches:
            - main
    workflow_dispatch:

env:
    REPO_PATH: ${{ github.repository }}
    GIT_REF: main
    NODE_VERSION: 20.20.0
    PNPM_VERSION: 10.8.1

jobs:
    # Job 1: Create version bump PR when changesets are merged to main
    changeset-pr-version-bump:
        runs-on: ubuntu-latest
        permissions:
            contents: write
            pull-requests: write
        steps:
            - name: Git Checkout
              uses: actions/checkout@v6
              with:
                  fetch-depth: 0
                  ref: ${{ env.GIT_REF }}

            - name: Install pnpm
              uses: pnpm/action-setup@v4
              with:
                  version: ${{ env.PNPM_VERSION }}

            - name: Setup Node.js
              uses: actions/setup-node@v6
              with:
                  node-version: ${{ env.NODE_VERSION }}
                  cache: "pnpm"

            - name: Install Dependencies
              run: pnpm install

            # Keep the committed Changesets config pointing at upstream for easier syncs,
            # but rewrite the repo at runtime so changelog generation resolves PR/author
            # metadata against the repository where this workflow is actually running.
            - name: Patch Changesets repo for current fork
              run: |
                  node -e "const fs=require('fs'); const path='.changeset/config.json'; const config=JSON.parse(fs.readFileSync(path,'utf8')); config.changelog=['@changesets/changelog-github',{repo: process.env.GITHUB_REPOSITORY}]; fs.writeFileSync(path, JSON.stringify(config, null, '\t') + '\n');"
              env:
                  GITHUB_REPOSITORY: ${{ github.repository }}

            # Create version bump PR using changesets/action
            - name: Changeset Pull Request
              id: changesets
              uses: changesets/action@v1
              with:
                  commit: "changeset version bump"
                  title: "Changeset version bump"
                  version: pnpm changeset:version # This performs the changeset version bump
              env:
                  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
